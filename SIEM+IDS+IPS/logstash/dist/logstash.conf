input {
    beats {
        port => 5044
        codec => json
        type => "SuricataIDPS"  
    }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]

    index => "logstash-%{+YYYY.MM.dd}"

  }

}